# Local dev only: point Vite at the production API Worker or leave unset for localhost:8787.
# Deployed Pages builds ignore this and call same-origin /api/*.
# VITE_API_BASE_URL=https://studyplanner-api.ben-tischberger.workers.dev
