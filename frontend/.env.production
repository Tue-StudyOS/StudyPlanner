# Vite loads this file for production-mode builds, including local npm run build.
# With VITE_API_BASE_URL unset, deployed hosts use same-origin /api/*.
# For a direct Worker build, set the variable in the shell or Pages build environment.
# VITE_API_BASE_URL=https://studyplanner-api.ben-tischberger.workers.dev
