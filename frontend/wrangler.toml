# Cloudflare Pages project config.
# VITE_API_BASE_URL is public build-time config, not a secret.
name = "studyplaner"
pages_build_output_dir = "dist"
compatibility_date = "2026-05-21"

# Deployed Pages builds use same-origin /api/* (see apiBaseUrl.ts). The value below
# is only a local-dev override when running Vite on localhost against production.
[vars]
VITE_API_BASE_URL = "https://studyplanner-api.ben-tischberger.workers.dev"

[env.production.vars]
# Intentionally unset — production resolves API calls relative to the Pages host.

[[services]]
binding = "STUDYPLANNER_API"
service = "studyplanner-api"

[[services]]
binding = "STUDYPLANNER_MCP"
service = "studyplanner-mcp"

[[env.production.services]]
binding = "STUDYPLANNER_API"
service = "studyplanner-api"

[[env.production.services]]
binding = "STUDYPLANNER_MCP"
service = "studyplanner-mcp"
